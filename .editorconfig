# https://editorconfig.org — keep HTML/JS as UTF-8 to avoid mojibake (?? instead of emoji/icons).
root = true

[*]
charset = utf-8
end_of_line = lf
insert_final_newline = true

[*.{html,htm,css,js,mjs,json,md,sql}]
indent_style = space
indent_size = 2
trim_trailing_whitespace = true
